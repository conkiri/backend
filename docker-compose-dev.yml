services:
  backend-dev:
    container_name: backend
    build: ./backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    networks:
      app-network:

networks:
  app-network:
    external: true
